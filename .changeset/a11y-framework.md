---
"@beatzball/litro": minor
---

feat: skip links API — `skipLinks` array on ShellOptions, `DEFAULT_SKIP_LINKS` constant, `SkipLink` type export

feat: SPA focus management — focus outlet after page swap, announce new page title via `aria-live` region
